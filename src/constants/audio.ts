export const DEFAULT_BGM_CONFIG = {
    loop: true,
    volume: 0.4,
};
export const DEFAULT_AMBIENCE_CONFIG = {
    loop: true,
    volume: 0.75,
};
export const DEFAULT_SFX_CONFIG = {
    loop: false,
    volume: 0.5,
};

export const CAT_AUDIO_COUNT = 1;
export const GUEST_AUDIO_COUNT = 4;

export const DEFAULT_BGM_KEY = "bgm";
export const DEFAULT_AMBIENCE_KEY = 'ambience';
