export type Task = {
  id: string;
  title: string;
  imageUrl: string;
  claim: string;
};

export type Achievement = {
  id: string;
  title: string;
  imageUrl: string;
  claim: string;
  totalInvitation: string;
};
